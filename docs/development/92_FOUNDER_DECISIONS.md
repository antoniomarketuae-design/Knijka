"# THE FOUNDER DECISION LIST — 2026-09-10\n\n**38 decisions are actually waiting on you.**\n\n- **15 block launch** (nothing sells, or nothing ships honestly, until you answer)\n- **12 block closing audit rows** — 34 of the 116 open findings, verified open today, cannot close in either direction without you\n- **11 are neither, but are cheap and get more expensive with time**\n\n**How I verified:** every item below was re-checked against the working tree today, not read out of a handoff. I re-counted `content/questions/**` (1,089 rows: 796 `approved`, 293 `needs-review`, 0 signed), ran the two failing test files (`Test Files 2 failed · Tests 2 failed | 26 passed`, 4.0 s), ran `freeze-lesson-carry.mjs --check` (12 withheld), ran `finding-reader.mjs --count` (`filed=1511 retired=1395 open=116 critical=47`) and grepped each cited finding id against the live open list, read the actual source lines for every predicate quoted, listed the `.env` key **names** (never values), stat'd the SSH key, listed `E:\\knijka-backups`, and hit the public GitHub API. **Four claims in the sweeps died on verification and are in the drop list at the bottom.**\n\n---\n\n# 1. BLOCKS LAUNCH (15)\n\nRanked by what it costs to leave undecided.\n\n### 1. Rotate the FlokiNET root SSH key — and decide whether the box is compromised\n**Q:** Re-key only (A), re-key + read the box's logs (B), or re-key + rebuild (C)?\n**Verified:** `C:\\Users\\Ljh\\.ssh\\id_ed25519_flokinet` still has mtime **2026-04-29 21:59** — never regenerated. GitHub API returns `"private": false, "visibility": "public"`, and `root@213.218.160.60` is committed in **4 tracked files** (`docs/development/61_TWO_DEV_COLLABORATION.md`, `tools/deploy/README.md`, `tools/deploy/pull-backups.sh`, `tools/upload-clips.sh`). So the host half is public and the key half was disclosed in a chat transcript.\n**Blocks:** nothing technically — which is why it has been flagged 6+ times and never done. It is root on the only machine holding the staging DB, the on-box backups and both deploy scripts.\n**After:** 20 min to re-key. +1–2 h for the log audit, +1 day for a rebuild.\n**Rider, same sitting, no separate decision needed:** revoke `POYO_API_KEY` (present in `platform/.env`; nothing under `platform/src`, `tools/` or `content/` reads it — you may simply delete the line), and move `SEED_FOUNDER_PASSWORD` out of plaintext (`platform/.env`, gitignored, never committed — local-disk exposure only).\n\n### 2. Rotate `founder@knijka.ai`, bump `sessionEpoch`, and prune the admin rows\n**Q:** Rotate + demote every admin except yourself (A), or rotate + keep the current admin set after reading it row by row (B)?\n**Verified:** `git log -S 'founder-dev' -- platform/scripts/seed-founder.mjs` → commit **904fb2a**, in a repo the API confirms is public. Exact commands already written in `docs/development/91_LAUNCH_BLOCKERS.md` §4.1 and §4.2.\n**Blocks:** every paywall. An admin row is a free licence to the €21.99 sim pack, unlimited mocks, the practice cap, the hazard gate and the tutor quota. Without the `sessionEpoch` bump, a session opened with the published default survives the rotation for 30 more days.\n**After:** 10 min, four commands.\n\n### 3. Sign the 29 first-aid questions — or say what must be checked first\n**Q:** Sign them yourself (A), have a clinician read them first (B), or reject and ship first aid with a hole (C)?\n**Verified today:** `content/questions/ptp-i-parva-pomosht.json` is **31 approved / 33 needs-review (48%)** against `MIN_APPROVED_SHARE = 0.5`. Both tests re-run and red: `content-bank.test.ts` (`REVIEW_DEBT: ptp-i-parva-pomosht: only 31/64 (48%) approved`) and `compose.test.ts:73` (`expected [ 'l-accidents-first-aid' ] to deeply equal []`). Four concepts sit at **0 approved**, so nothing partial helps.\n**Blocks:** the only two red tests in the whole suite — so "the gate is green" is not a usable signal for anything. And `l-accidents-first-aid` is the only lesson of 54 that cannot compose a quiz beat.\n**After:** 1.5–3 h of your own reading at `/review`, ~2–3 min a row. Zero engineering — the status flip *is* the change and both tests go green on it.\n**Second half of the same decision (do not skip):** which authority grounds first aid, and whether `lawRefs` may stop being mandatory (`LawRefSchema` is `.min(1)`, which is what forced the decorative чл. 123 citations in the first place — doc 90 §14.8 **M** and **N**). Sign without answering this and nothing can re-verify the rows afterwards; the next edit breaks the grounding with no test to catch it.\n**TRAP, in writing at `91_LAUNCH_BLOCKERS.md:723`:** do **not** approve only the 4 non-medical rows in that file. It reaches 55%, turns `content-bank.test.ts` green, leaves every first-aid row withheld and `compose.test.ts` red — a true red made green by crossing a threshold.\n\n### 4. The recovery position — ratify the 2025 consensus or follow what БЧК examines\n**Q:** Ratify ERC/RCUK 2025, i.e. do **not** roll a trauma casualty (A), or match what the Bulgarian Red Cross teaches and examines (B)?\n**Verified:** `content/medical/claims.json` → `med-recovery-position`, `statusBg: "contested-content-affected"`, `questionIds: ["q-ptp-022","q-ptp-037"]`. Written up in `content/medical/README.md` and `docs/education/92_FIRST_AID_SOURCES.md:696`.\n**Blocks:** item 3. Signing `q-ptp-022` **is** taking this ruling, silently. Take it first.\n**After:** ratify = free, the content is already written that way. Reverse = rewrite 2 rows + update `conflicts[]` + re-run `content/medical/tools/verify-claims.mjs`; half a day.\n\n### 5. May theory practice keep dealing unreviewed rows to students?\n**Q:** Keep it (A), flip the default so practice matches the classroom (B), or label them „под преглед" in the UI (C)?\n**Verified:** `platform/src/modules/learning/session.ts:106` `includeUnreviewed = true`, `:143` adds `needs-review` to the allowed set. The exam (`builder.ts:119`) and the classroom both require `approved`. Same rows, three policies, one of them accidental.\n**Blocks:** the honesty of item 3. Today all 293 unreviewed rows — including all 29 unsigned CPR/tourniquet/casualty-handling rows — reach a 17-year-old through `/practice` with full `explanationBg`, while the classroom refuses to speak the same sentences. Rejecting a first-aid row keeps it out of the exam and the lesson and it still appears in practice.\n**After:** the flip is one line plus the tests that pin current behaviour (1–2 h). The labelled option is a day.\n\n### 6. How many of the 1,089 rows must carry your signature before launch?\n**Q:** Sign the 135-row exam floor and flip the predicate (A), sign deeper (B), launch on the unsigned string with the ceiling frozen (C), or sign all 1,089 (D)?\n**Verified:** `content/review/approvals.json` → `"entries": []`, baseline 837, frozen 2026-08-03. **0 of 1,089 signed.** `builder.ts:119-121` is still `return q.status === "approved";`. `isHumanApproved()` exists at `content-admin/approvals.ts:129` and works, but flipping it today makes the eligible pool zero and no mock exam can be built at all.\n**Blocks:** the product's central claim. An "official-format mock exam" is currently composed from a word a generator wrote 1,005 times — the same word that was on 22 of the 24 rows with a wrong answer key.\n**After:** the 135-row floor is 5–8 h of your reading + ~1 h engineering (one line in `builder.ts`). All 1,089 is a multi-week programme needing its own plan.\n\n### 7. Which legal form sells this, and fill the five identity constants\n**Q:** Natural person (A), ЕТ/registered freelancer (B), or ЕООД/ООД (C)?\n**Verified today, all still placeholders:** `platform/src/lib/legal/identity.ts` — `SELLER_KIND = "naturalPerson"` (:48), `ENTITY_ADDRESS = "[АДРЕС ЗА КОРЕСПОНДЕНЦИЯ]"` (:85), `CONTACT_EMAIL = "[ИМЕЙЛ ЗА КОНТАКТ]"` (:88), `LAST_UPDATED = "[ДАТА]"` (:91). The gate is wired: `payments/stripe.ts:13` imports `legalIdentityGaps`, calls it at `:110`.\n**Blocks:** every euro. Also blocks `.bg` (needs a Bulgarian company/trademark), the DPAs (they need a counterparty) and the VAT question.\n**After:** registration is days-to-weeks. The code change is 5 minutes and lights up all ten `mailto:` links across /terms, /privacy, /contact and the checkout consent gate.\n\n### 8. Start the Stripe activation clock\n**Q:** Activate now as a natural person and re-register later if you incorporate (A), or wait for the company (B)?\n**Verified:** `platform/.env` contains **no `STRIPE_*` key at all**. Doc 54 §7: activation "takes days, start early".\n**Blocks:** revenue. This is the item most likely to be what you are *waiting on* during launch week rather than working on.\n**After:** 30 min of your time, several days of Stripe's. Then 20 min for the four env vars + the live webhook — whose `whsec_` must come from the **live** endpoint's own screen (`91_LAUNCH_BLOCKERS.md` §4.5 names copying the test secret as the exact failure B13 exists to catch).\n\n### 9. Buy the domain\n**Q:** `knizhka.ai` (~$70–100/yr, 2-yr min, availability never checked), `knizhka.app`, `knizhka-ai.com`, or wait for `.bg` (needs item 7)?\n**Verified:** `docs/development/54_DEPLOYMENT_STRATEGY.md:112-116`. `NEXT_PUBLIC_APP_URL` is still `http://localhost:3000` in `.env.example`; `docs/00_PRODUCT_MAP.md:39` still has the named tunnel as an unchecked box.\n**Blocks:** the most upstream item on the list — the Stripe webhook URL, the password-reset link, `MAIL_FROM`, and SPF/DKIM all need it. Also fixes a live embarrassment: the trycloudflare staging URL rotates on every restart and Chrome flags it "Dangerous", so anyone you demo to sees a warning at an address that has since changed.\n**After:** 30 min to buy and point DNS; ~1 h engineering for the named tunnel and env vars.\n\n### 10. Open the transactional-mail account\n**Q:** Resend (A) or Postmark (B)?\n**Verified:** `platform/.env` contains **no `MAIL_*` key**; `.env.example` still says `MAIL_TRANSPORT=console`.\n**Blocks:** hard-gated exactly like the legal identity — `isStripeConfigured()` is false while these are unset, so buy buttons render „Скоро". The product deliberately refuses to sell what it cannot give back: take €12.99 in September, no reset link in October.\n**After:** 20 min + DNS propagation; then three env vars and a `pm2 restart`. Chained behind item 9.\n\n### 11. Where does production actually run?\n**Q:** Vercel Pro + Neon as doc 54 recommends (A), the FlokiNET VPS as actually built (B), or Vercel for the app and the VPS as staging only (C)?\n**Verified:** doc 54 §4 recommends Vercel; every ops script, backup timer and deploy path in the repo is written for the VPS; `docs/80_FULL_AUDIT_2026-07-24.md` M-25 records that `privacy/page.tsx:129-138` names Vercel + Neon while the running system is a VPS behind a Cloudflare quick tunnel.\n**Blocks:** three items downstream — which DPAs to sign, what the privacy policy's processor list must truthfully say, and where the Stripe/mail env vars get set. Deciding it late means redoing all three. It is also a standing monthly bill, which is why it is yours.\n**After:** zero if you stay on the VPS; ~1 day to stand up Vercel + Neon and re-point everything.\n\n### 12. Commission the legal review, then drop the draft banner\n**Q:** Full Bulgarian consumer + GDPR review before launch (A), targeted review of the three riskiest clauses (B), or launch with the banner up (C — defensible for a free tier, not while taking money from minors)?\n**Verified:** `<DraftBanner />` is still rendered at `platform/src/app/(legal)/layout.tsx:48`. `docs/legal/50` §8 item 2 assigns it to "Founder + lawyer"; §7 item 4 says the premium-sim pro-rata refund clause was "drafted as good faith — founder/lawyer to confirm".\n**Blocks:** you would be selling to 17-year-olds under terms that publicly announce they are a working draft. The withdrawal-waiver position decides whether a buyer can demand a refund 13 days after finishing the course.\n**Fold into the same engagement:** **ADR-004** is still `Proposed` (`docs/architecture/07:37` — "confirm after legal review"). It is the one ADR governing a High-impact regulatory risk (EU AI Act / GDPR, minors, emotion recognition) and has never been confirmed by anyone qualified to.\n**After:** 1–2 weeks and a few hundred euro, externally. Removing the banner is one line.\n\n### 13. Execute the DPAs — and first decide which processor list is the true one\n**Q:** Sign for the documented set (Vercel, Neon, Stripe, Anthropic) (A), or the real set (FlokiNET, Cloudflare, Stripe, Anthropic + mail + TTS) (B)?\n**Verified:** `docs/legal/50` §8 item 3; the mismatch is `80_FULL_AUDIT` M-25 against `privacy/page.tsx:129-138`.\n**Blocks:** you are the controller for minors' data. No DPA is a direct Art. 28 failure, and the policy currently names processors that are not handling the data — separately a false statement to the data subject. Downstream of item 11.\n**After:** half a day of vendor portals.\n\n### 14. Confirm the VAT / tax posture\n**Q:** Register for VAT now (A), stay under the threshold and confirm „цените включват всички данъци" still holds (B), or change the wording (C)?\n**Verified:** `docs/legal/50` §8 item 9, "Founder + accountant". The claim is rendered on the pricing and checkout surfaces.\n**Blocks:** a tax statement made to consumers that code cannot check. Downstream of item 7.\n**After:** one accountant conversation; a copy edit if the answer changes.\n\n### 15. Set the launch polish bar — the 512 UNPOLISHED findings\n**Q:** Ship as-is and make it a post-launch backlog (A), fix the 123 major only (B), or pick themed groups (C)?\n**Verified:** `docs/simulation/88_LESSON_AUDIT.md:4454` — "512 findings, 123 major and 376 minor. The founder rules on these, so every one carries its frame and he can open it." This bucket is **separate** from the 1,511-row BROKEN corpus and has never been worked, by standing instruction. Themes: U1 a city with no people 110 · U12 audio 99 · U3 weather/light 84 · U5 cockpit 57 · U4 boxes 37. Register row **B65** ("the Map its very Raw, boring", still `PARTIAL-SEEN` at line 3710) is the same call.\n**Blocks:** "done" has no definition for visual quality, so the polish backlog can neither be scheduled nor declared finished — and the honest open-observation total is **628**, not 116, which is invisible in every status report you get. Several are not cosmetic: a briefing that promises standing water on dry asphalt teaches the student to stop trusting the briefing.\n**After:** ruling is an afternoon with the doc (it is grouped for exactly this). "Acceptable" closes 512 rows in one commit. "Defect" is a multi-week world-building programme.\n\n---\n\n# 2. BLOCKS CLOSING AUDIT ROWS (12)\n\n**34 distinct open findings, verified against the live open list today,** cannot close in either direction without you. That is 29% of the 116 open rows. Verifiers keep re-measuring them and keep writing "this is a founder question, not a measurement" — pure waste.\n\n### 16. Is a fault free the first time — and is the grace per fault or per topic? — **16 rows**\n**Q:** Keep the universal grace (A); always charge a lesson's **own** central offence from the first commission (B); or charge everything from the first time and keep the teach card as explanation only (C)? Second half: one free lesson per **topic** (current) or per **fault code**?\n**Verified open today, all 16:** `sc-pk-busstop-ban:105f805c` `:b103c282`, `sc-signal-flashing:0d68b149`, `sc-signal-hesitation:440b1f7c`, `sc-vp-readiness:54c815da` `:b3c922d5`, `sc-follow-tailgater:63c0c28c` `:f42dce4f`, `sc-vu-emergency:4056508c`, `sc-ov-solid-line:3436a5e7`, `sc-ac-truck-spray:990e5f64` `:8ed4d8b3`, `sc-jx-priority-confidence:9c987e7b`, `sc-ed-d2-city-run:a0bdad4b`, `sc-vp-police-stop:44cfeff6`, `sc-ac-crosswind:a9db1738`.\n**Where:** `platform/src/modules/sim/scenarios/coach.ts` (the override is folded in around :280) · `scenarios/policy.ts` already has the per-code channel (`policyForViolation`) · `rules/engine.ts` `SPEED_REGRADE_SEC = 6`.\n**Cost of not deciding:** the shipped behaviour is that a deliberately-wrong drive **passes the lesson built to catch it**. `sc-follow-tailgater`'s scripted wrong drive returns ИЗДЪРЖАН, 2 т., 2 stars, 0 опасни, both route tasks ticked. `sc-pk-busstop-ban` credits „Подмини цялата зона на спирката, БЕЗ ДА СПИРАШ В НЕЯ" thirty seconds after the product's own card said he stopped in it. That is the north-star test failing in the most direct way available.\n**After:** the ruling is one sitting. (B) or (C) is a day or two — the override channel and the re-grade family already exist — plus a sweep to re-photograph the 16.\n\n### 17. Does «задачата иска ≤N» actually bill the student? — **1 additional row + 2 shared**\n**Q:** Enforce the objective cap as a graded ceiling with its own code and lawRef (A), or demote it and change the copy so it stops claiming precedence (B)?\n**Verified:** `grep -rn "taskCap" platform/src/modules/sim/rules/` returns **nothing** — the figure never reaches the rule engine. Meanwhile `StatusDashboard.tsx:648` carries the comment "THE NUMBER THE STUDENT IS ACTUALLY BILLED AGAINST" and `:661` prints „· задачата иска ≤{bindingKmh}". Rows open today: `sc-ac-truck-spray:990e5f64` (critical, 127–131 км/ч in a task asking ≤80, finishes 0 нарушения, +40 XP, debrief praises "чисто каране"), `:8ed4d8b3`, and `sc-vu-pass-clearance:260b13fd` (critical — ЗАДАЧА ticked at 59 км/ч past the cyclist).\n**Cost:** a promise on the glass the scoring does not keep, on a safety-shaped number. The student learns the amber ceiling is furniture.\n**After:** (B) is hours — copy plus one test. (A) is a day+ and needs a `ViolationCode`, an N38 basis and a lawRef you sign, and it changes what lessons can fail you for — which is why it is not an engineer's call.\n\n### 18. Are the absent rear camera / proximity read-out / cluster dials defects or pedagogy? — **3 rows**\n**Q:** Defects, build them (A); deliberate, and the product must **say so** on the glass (B); or split (C)?\n**Verified open:** `sc-ed-reverse-line:1f812456`, `sc-ed-reverse-line:e05f2cee`, `sc-pk-stop-vs-park:e788ce46`. The ruling is already written in a source comment at `engine/reverseView.ts:95-104` — "the absence is the lesson. Overturnable by the founder" — and a verifier correctly refused to accept it as a repair ("A ruling in a comment is not a repair").\n**Cost:** whichever way you rule, the product is currently **silent** about the choice. «Наблюдение» is graded on a shoulder check with no camera and nothing tells the student that is the point — a requirement-zero (THEO-4) breach either way.\n**After:** (B) is ~2 h — three authored sentences and three closures. (A) is ~1 week and needs a settings gate so the camera is never the default view of an exam manoeuvre.\n\n### 19. Does the un-panelled HUD register keep its no-plate treatment? — **8 rows**\n**Q:** Keep the ghost register as signed-off design (A), add a conditional scrim below a contrast ratio you name (B), or restore plates (C)?\n**Verified open:** `sc-ac-snow:55ddf562`, `sc-ac-crosswind:4607edf0` `:8f42504c`, `sc-ov-solid-return:70845fcc` `:5cb8eb40`, `sc-junction-gap:df95401c` `:e87d5be1`, `sc-zebra-approach:2c75cf8f`. Code at `components/sim/lesson-ui/PlayAreaStyles.tsx:2303` (`GHOST_SURFACES`) and `:2327` (`UNPANEL_CSS`).\n**Cost:** this is your own design call (pop-ups are "much much annoying"), and the audit will not overrule it — so eight rows loop forever, every HUD repair lane must be told not to "fix" it, and on a night city the instruction the student is graded against is genuinely hard to read. The number on the table is 1.13 : 1 inside a 44 px control.\n**After:** (A) minutes, rows retire. (B) a day — the contrast measurement already exists in the test suite; it is wiring plus a threshold you name.\n\n### 20. At assistance level 1, does the controller billboard print the verdict or only the cue? — **1 row (major)**\n**Q:** Keep it printing the answer (A), name the posture but withhold the verdict until he acts (B), or cue-only with the verdict after the attempt (C)?\n**Verified open:** `sc-sig-controller-postures:3936550e`. At 04-t048s the card headline is literally „МИНАВАШ ТИ", with „Предимството е ТВОЕ" and „Виждаш го СТРАНИЧНО" reading the posture *for* him — while the task is „Премини кръстовището, когато позата разреши посоката ти".\n**Cost:** the one perception the north star cares about in this lesson — reading a human being's posture — is removed by the aid, on the rung most students start on. It cannot close either way without you, because doc 86 §12.5 records this bubble as **your third-most-repeated ask** and a repairer must not resolve it by deleting your feature.\n**After:** (B) is under a day — one card's copy and reveal timing, plus a trace re-record.\n\n### 21. May a drive that takes 3.2× the reference time still be ИЗДЪРЖАН with three stars? — **4 rows**\n**Q:** Leave it, Наредба 38's own pass test governs (A); author a quality component that can move stars without inventing an offence (B); or let par gate the stars but never the verdict (C)?\n**Verified open:** `sc-vu-emergency:9e72c8bd`, `sc-vu-emergency-junction:853790f7`, `sc-rb-busy-gap:8f50287b` `:a6f83f6b`. `lessons/scenario/rubric.ts` states par time is "INFORMATIONAL ONLY, never affects stars"; the only catalogued slowness offence is motorway-only, so convicting an urban crawler would mean inventing an offence code and a lawRef — an ADR-002 breach. That is why an engineer cannot settle it.\n**Cost:** the product hands „Точно това иска да види изпитващият" to a student who drove three times slower than reference (238 s against a 75 s ориентир). A false certificate about the real exam. Your own complaint reappears here: the most careful drive of four — 15 stops, 43 s of lawful waiting — scores the **worst** at 21 т.\n**After:** (A) minutes. (B) two to three days — a new authored component per template family plus its own false-refusal gate.\n\n### 22. What happens when a student drives off the edge of the built world?\n**Q:** End the drive with a stated non-punitive reason (A), warn-and-wall only (B), or end it **and** grade it as leaving the authored route (C)?\n**Verified:** `runtime/district.ts:456`, verbatim — "IT STILL ENDS NOTHING — no lesson terminates on a clearance… That consumer is `lessons/finish.ts`, which this lane does not own, and it is not written." The previous attempt (`terminalDepartureZone`) was **disarmed for producing a false refusal**, which is exactly why the next attempt needs your rule rather than an engineer's default.\n**Cost:** this is upstream of the whole ledger, not one family. Drives end inconsistently, and inconsistent endings corrupt every verdict the audit is built on — a leg that runs the clock out and one that stops at the belt produce different sheets for the same driving.\n**After:** a day or two, and it must ship with its own false-refusal guard.\n\n### 23. Are landscape phones in scope? — **1 row (major)**\n**Q:** In scope, fix the login form (A), or out of scope and the row retires by ruling (B)?\n**Verified open:** `app-login:e2577ced`. Measured in WebKit on the exact viewport the harness drives (852×393): `#password` bottom 399 — below fold; submit button bottom 459, `elementFromPoint` returns **null**. Signing in took three scroll-and-stabilise cycles.\n**Cost:** there is no `/simulator` route for `app-login`, so every sweep reports it "never driven" forever — structurally immortal until you rule. Meanwhile a 17-year-old on a sideways phone scrolls blind twice on the product's first screen. Note the harness fix (`b224c7e` raised the click timeout) "buys a machine time and buys a student nothing".\n**After:** "out of scope" is one sentence. "In scope" is a few hours plus a viewport test.\n\n### 24. The no-evidence bucket: build a reproducible drive, accept and ship, or build rate mode?\n**Q:** Spend one instrument wave making the audit drive travel the same route and distance every time (A), accept the unmeasured rows and ship (B), or build rate mode (C)?\n**Verified:** open = 116. I could **not** re-derive the STILL/UNJUDGED split with `finding-reader.mjs` today (its flags are `--count --all --file --unrouted --filed`); the sweeps report 46 and ~66 respectively, so treat the size as "roughly 40–55% of the open list has no evidence at all". The premise that decides it **is** verified in `founder-batch.md` §4: same lesson, same leg, same commit `32505eb5`, four minutes apart — 07:25 canary НЕИЗДЪРЖАН score 31 / 32 frames / 205 s vs 07:29 sweep НЕЗАВЪРШЕН score 1 / 19 frames / 115 s. That is variable **exposure**, not grading noise, so rate mode would faithfully record how far the harness happens to get and call it a property of the lesson.\n**Cost:** this is the programme's stopping condition. Without it every wave spends its budget re-driving rows the instrument structurally cannot settle, and the open count moves by ones.\n**After:** (B) free. (A) is one instrument wave, 2–3 days — and it is a harness change, so it closes nothing by itself; every row it unblocks still needs a product repair named against it.\n\n### 25. B16 — which roundabout defect did you mean?\n**Q:** The drivable boundary (A — fixed, close the row) or the ring geometry (B — regenerate and pay the re-record)?\n**Verified:** row **B16** is still `BROKEN-SEEN` at `docs/simulation/87_FOUNDER_ITEM_REGISTER.md:3661`. The unfixed half: four mouths eat **208° of 360°** on `rb-mini`, because the generators author 16.25 m arms into an 18 m ring.\n**Cost:** one of only two BROKEN-SEEN rows sits open, and four roundabout districts are frozen because nobody will spend the 36-trace re-record on a guess.\n**After:** (A) zero. (B) 1–2 days including 36 committed traces re-recorded.\n\n### 26. B67 — the motorway speed ceiling\n**Q:** Take the derived 160 km/h candidate on Напреднал (A), grant the full 180 (B), or hold at today's 96.6 on a 90 road and answer in copy (C)?\n**Verified:** row **B67** is still `PARTIAL-SEEN` at `87_FOUNDER_ITEM_REGISTER.md:3712`; the cockpit half is already NOT-A-DEFECT.\n**Cost:** reaching 160–180 on Нормален means letting a student cruise **2.5–4× the опасна threshold** over the posted motorway limit, on a product whose exam fails him for 10 over. That trade is a syllabus decision. Meanwhile the tuning fix that makes dangerous speeding *committable at all* on the lower tiers is entangled with the number you have not set, so nobody lands either.\n**After:** one constant plus a re-sweep of the speed traces — a few hours.\n\n### 27. The 129 unread universal-negative law claims\n**Q:** Review them (or delegate the reading and sign the result) (A), accept the freeze and ship on the ceiling-only guarantee (B), or delete every unverifiable "the law does not…" sentence rather than reading them (C)?\n**Verified today:** `content/law/claim-ledger.json` holds **144 pins — 15 accepted, 129 inherited, 0 open-defect**. **Caveat: the file is UNTRACKED** (`?? content/law/claim-ledger.json`) — a session is mid-flight on it right now. Confirm it lands before acting.\n**Cost:** 129 sentences telling a student what Bulgarian law does *not* say, none of them read by anybody. That is the same shape as the invented 50-metre level-crossing rule the bank itself calls „ИЗМИСЛЕНО ЧИСЛО, най-тежкият единичен дефект в банката", which survived four correction rounds by hiding just past a comma. The gate stops new claims; it certifies nothing about the standing ones.\n**After:** (B) is a sentence. (A) is a day or two, batchable by file.\n\n---\n\n# 3. NEITHER, BUT DECIDE (11)\n\n### 28. Read and clear the 12 withheld concept summaries\n**Q:** Clear all 12 (A), only the lessons you ship first (B), or leave them silent (C)?\n**Verified by running `node platform/scripts/freeze-lesson-carry.mjs --check` today** — exactly 12 withheld: `c-accident-duties, c-e-scooters, c-elderly-disabled, c-emergency-lane-breakdown, c-general-care-duty, c-mandatory-equipment, c-parking-prohibitions, c-railway-crossing, c-roundabout-behavior, c-roundabout-rules, c-scene-safety, c-when-call-police`. `clearanceCarry.ts:361` `MACHINE_SIGNERS` bans `claude, agent, ai, bot, script, ci…` — **this cannot be delegated to me.**\n**Cost:** those beats say „под преглед" where a teaching sentence should be. Nothing is broken; the classroom just teaches less, indefinitely.\n**After:** 30–60 min total. `--show <id>`, read the sentence, `--clear <id> --pin <fingerprint> --by "<your name>"`. One row per invocation by design — the bulk repin mode was deleted after it certified 11 rows nobody had read. **Do item 3 first:** `--propose` then prints which concepts became eligible once the 29 are signed.\n\n### 29. Hazard: watch the 8 clips and approve — or say the section does not ship yet\n**Q:** Watch and approve (A), ship with hazard turned off (B), or approve the items and leave the pack question open (C)? Separately: **which pack carries hazard training** — `access.ts` says encoding a guess "would silently create a product tier nobody agreed to".\n**Verified:** all 8 items in `content/hazard/items.json` are `needs-review` (`hz-zebra-hot-approach, hz-living-zone-entry, hz-parked-row-child, hz-bus-pullout, hz-cyclist-clearance, hz-obstacle-oncoming, hz-tram-left-turn, hz-brake-dont-swerve`); the serving gate is `approved`-only.\n**Cost:** Door #2 — the section the pitch calls the safety differentiator — deals **zero items** to a paying account. Clips are rendered, windows computed and frozen, scoring engine written and tested. The only missing thing is a human confirming eight cuts.\n**After:** ~1 h of watching + ~15 min of engineering (there is no approval UI; the status is hand-edited). Watch `hz-obstacle-oncoming` and `hz-tram-left-turn` twice — their clips were re-cut and carry a warning.\n\n### 30. Write to БЧК / the Ministry for the учебна програма\n**Q:** Send the request (A), ship the five claims marked ungrounded with the tutor saying so (B), or drop the affected questions (C)?\n**Verified:** `content/medical/claims.json` — 21 claims, of which **5 are ungrounded**: 1 `teaching-material-only`, 3 `inferred-only`, 1 `no-reachable-source`. `content/medical/README.md:229`: obtaining it is "the single highest-value acquisition left in this domain"; Наредба № 24 contains no clinical value at all — no depth, no rate, no ratio.\n**Cost:** two of the five describe a **physical technique** we teach on inference (the Rautek grip, helmet handling). Signing those rows in item 3 means signing an ungrounded technique. Lead time is weeks — **send the letter before the signing session, not after.**\n**After:** your time to write it, then weeks outside our control. ~1 day of engineering if it arrives.\n\n### 31. The 14 named rows A–K in doc 90 §14.8\n**Q:** Rule row by row in one sitting (A), commission the missing artefacts instead (B), or withdraw the affected rows (C)?\n**Verified:** 293 `needs-review` rows exist and the §14.8 table is present at `docs/education/90_THEORY_VS_LAW_AUDIT.md:1068`; I did not re-check all fourteen ids individually. Each already carries its evidence and each is a dead end for retrieval — e.g. **C** `q-speed-049`: "Our наредба copy predates ЗДвП чл. 62а. Unretrievable, not merely unretrieved." **D**: the наредба and the statute disagree on the дрегер penalty and your standing rule forbids us resolving it. **J** `q-signs-041`: group Т has only Т1/Т2/Т10/Т13/Т15 on disk, so the Т6/Т7 faces need commissioning.\n**Cost:** fourteen rows nobody can move by looking harder. Two also hold why-panel tests and a sign-face commission hostage.\n**After:** one sitting to rule; 1–2 days engineering if you commission the Т13 drill or the sign faces.\n\n### 32. The Blender body pack — the officer (FR-35) and the children (FR-43)\n**Q:** Sit live Blender sessions yourself (A), buy/commission rigged low-poly humans (B — ADR-001 constrains vehicles, not people), or close B41/B52/B60 as won't-fix (C)?\n**Verified:** rows **B41** (:3686), **B52** (:3697, `BROKEN-SEEN`), **B60** (:3705) all still open in the register. B52: "the same primitive rig as every adult, scaled — featureless sphere head, capsule torso… at the 21 m the drill is played from it is a washed-out blob."\n**Cost:** in a product whose whole claim is safer real drivers, the **vulnerable road users are the least real thing on screen.** Frozen since 2026-08-02. Blind headless sculpting was already proven to fail.\n**After:** half a day per rig live. Note the `blender` MCP server failed to connect this session (CONNECTION_CLOSED) — it needs a restart before that work can start.\n\n### 33. The tutor: turn it on, and hold the Bulgarian voice audition\n**Q:** Supply an Anthropic key so the tutor can speak at all (yes/no); then Kalina (F) or Borislav (M); one voice or two; and does the teacher read quiz text aloud?\n**Verified today:** `ANTHROPIC_API_KEY` in `platform/.env` is **empty** (length 2 — bare quotes). No Azure keys anywhere. Doc 84: Azure has exactly two bg-BG voices, "and 'the teacher' is a character choice, not a config value". Doc 81 calls a Bulgarian voice "the single highest-effect variable in this whole document, and the single biggest unmitigated risk".\n**Cost:** the AI tutor — half the product's name — has never been heard by anyone, and phase 2 classroom authoring is correctly stalled behind you (doc 81 lists commissioning a character before the audition as a named anti-pattern).\n**After:** turning the tutor on: ~2 h, ~$0.20. The audition: 3–4 h, under $2, plus five 17-year-olds. If approved, the full render is $9.69 at the westeurope meter and needs `--allow-spend`. **Captions-only is explicitly an acceptable outcome, not a failure.**\n\n### 34. FR-19 clips: authorise ~485 credits, and rule on the `check-surroundings` variant\n**Q:** Ship the recommended 0-credit programme on our own engine (A), authorise the decisive probe suite #5–#8 at ~485 cr ≈ $2.43, 7% of balance (B), or authorise the 405-cr generative fallback (C)? Separately: may `check-surroundings` teach the **sight line** instead of the child — which weakens «дете» to «препятствие»?\n**Verified:** `docs/simulation/90_FR19_CLIP_PRODUCTION_SPEC.md` — balance 6,873.5 credits ≈ $34.37; §1.6 is the content ruling; the spec's own arithmetic says a generative thirteen would cost ~14,000 credits, twice the balance ("not a preference; it is arithmetic").\n**Cost:** low — 13 tutorial cards ship as stills and the spec says that is acceptable. The real loss is that the ADR-001 question (does a supplied first frame hold **our** fictional car, or does the model photorealise it into a real brand?) stays unanswered for $0.83.\n**After:** probes ~2 h. Wave 1 of six own-engine clips ~2 days, currently blocked on a separate engineering issue (`/dev/clip-headless` fails to warm on this HDD).\n\n### 35. GDPR paperwork: three retention horizons + the ROPA\n**Q:** R1 — NULL `StripeEvent.payload` past Stripe's ~120-day dispute window, yes/no? R2 — drop `AdminAction.detail` after N months, and what is N? R3 — inactive-account horizon (the doc suggests 3 years, notify-then-delete). And: produce an Art. 30 ROPA, yes/no?\n**Verified:** `91_LAUNCH_BLOCKERS.md` **§4A** (line 745) exists and records all three as deliberately not built; `prisma/schema.prisma:515` `StripeEvent` (no `userId`, no relation — so no Art. 17 cascade reaches it) and `:119` `AdminAction`. Both tables hold **zero production rows** today.\n**Cost:** this is the **only item on the whole page that gets permanently more expensive with every day of delay.** Free now, a migration over real buyers' data later. "Forever, including their Stripe session id" is a choice nobody has made out loud.\n**After:** ~4 h engineering per rule once stated (a cron and a bounded update). The ROPA is ~2 h to draft from the §2–§3 tables; the signature is yours.\n\n### 36. The public-exposure decisions: repo visibility, the committed host, the git history\n**Q:** Stay public and redact the host into an env var (A), go private (B — closes all three at once), or stay public and accept both (C)? And separately: rewrite the history with filter-repo (breaks every clone, including the second developer's and the `ledger/audit` branch's tree hashes), or accept?\n**Verified today:** GitHub API — `private: false`, `visibility: public`. `root@213.218.160.60` in 4 tracked files. `git log -S 'founder-dev'` → 904fb2a, and it always will unless rewritten.\n**Cost:** publishing the exact root endpoint of the only machine holding student data narrows an attacker's work to the key — which is item 1. Once items 1 and 2 are done, the rest is a reputation call, not a live hole. It gets harder the more clones exist, so decide before you add anyone.\n**After:** 1 min to flip visibility; 30 min to redact (`pull-backups.sh` already reads `KNIJKA_VPS`); 2 h + re-coordination for a history rewrite.\n\n### 37. Four ops authorisations — four yes/no answers, one sitting\n1. **Branch protection** — require the `gate` check and forbid direct pushes to `scenario-engine`/`main`? Owner-only, browser UI (`gh` is not installed here). Until then staging auto-deploys a red commit from either developer on a 5-minute tick and the whole test suite is advisory. **5 min.**\n2. **Third backup copy off this machine** — name a target. **Verified:** the newest local dump in `E:\\knijka-backups` is `knijka-20260829T031501Z` — **12 days old**, while the VPS has dumped daily throughout. Repo, audit ledger and backup share one 7200 rpm HDD. **Under an hour once named.**\n3. **May I create a Windows scheduled task** for the pull? It is persistent configuration on your machine, so it was deliberately not created. The 12-day gap proves the manual pull does not survive a busy fortnight. **5 min with your word.**\n4. **Uptime monitor on `/api/health`** — yes/no? The deploy health-gates and rolls back; nothing catches the box dying alone. It already happened: staging down 11.5 h from 23:03 on 07-19, fixed only by an unrelated push. **15 min.**\n   *(Fifth, folds into item 1's re-key: does the second developer get his own key in `authorized_keys`, or does he work through GitHub only? Never share the flokinet key — that sharing is what made item 1 as bad as it is.)*\n\n### 38. ADR-006 — accept or reorder the four Scenario Studio stages\n**Q:** Accept 1→2→3→4 with physics last behind a feel-regression harness (A), pull map-data-layer v2 forward (B), or stop and rely on the exam bank for breadth (C)?\n**Verified:** `docs/architecture/07:55` still reads `2026-07-16 · **Proposed** (CTO analysis; founder to accept/reorder the stages)`.\n**Cost:** stage 2 is the schema change that answers register row **B64** (a placeable street-furniture kind) and part of the UNPOLISHED bucket, but nobody starts a staged programme whose order is unratified. Seven weeks old and possibly partly overtaken — worth a 10-minute triage, not a full reading.\n**After:** ruling 10 min; each stage 1–3 weeks.\n\n---\n\n# NOT ACTUALLY BLOCKED ON YOU — what I dropped, and why\n\nEach of these appears in a sweep or a doc as founder-blocked. Evidence settles them, so they are engineering work and they are off your list.\n\n- **The 77 unapproved signs.** `signClearance` refuses all 77, but running the resolver over all 511 beats produced **zero** sign refusals — `resolve.ts:233` says "No composed beat carries a sign board today". Signing them is on no critical path until a lesson first speaks a sign meaning.\n- **All 54 lessons being `draft`** (verified 54/54). `lessonNarration()` gates on `approved`, but `setLessonNarrationProvider` is **never called in production code** — approving all 54 today would change nothing on screen. Wire the provider first. (The roundabout re-cite that doc 90 **L** demands before any approval is retrieval work; only the framing choice — state the rule vs show the two-step derivation — would ever be yours, and it can wait for the fix.)\n- **The 4 non-medical rows in the ptp file** (`q-ptp-009, -044, -050, -052`). Ordinary content review; softening an unconfirmed citation is a one-line edit an engineer makes. Doc 91 §4.17 warns in writing not to approve *only* these four.\n- **A new «left the carriageway» offence code needing your signed lawRef.** Already resolved by retrieval — `rules/catalog.ts:574` carries `OFF_CARRIAGEWAY` citing ЗДвП чл. 15, ал. 1. Two sweeps still list this as yours; it is not.\n- **Whether WRONG_WAY may fire on unsigned one-way edges.** Resolved — the detector now asks the painter's own `scenarioSignScale` question, imported rather than mirrored.\n- **`sc-vp-stall`'s transmission channel** (listed as "his call" in `90_HANDOFF` §9.6). **Verified: zero `sc-vp-stall` rows in the live open list today.** Settled.\n- **The three reconciled speed numbers in the rail**, and the ILLEGAL_STOP card citing an unseen В27 — closed by evidence / fixed in 2618090.\n- **The zebra parking bays, the POINT-test ban span, and the drill whose instruction contradicted its grader** — all three were real; the first is the drill's designed trap, the second was fixed in 2618090 by measuring the vehicle body against the span, the third is repaired at `templates-following.ts:1786`.\n- **Register rows A8, B2/B19, B64, C1.** A8 is a reachZone scaling measurement; B2/B19 is authoring against a requirement you already gave (FR-13); B64 is a schema change (it becomes yours only through ADR-006); C1 is a 44 px touch-arc geometry fix.\n- **The 43 UNOWNED rows and the harness capability gaps** (no steering on `-wrong` legs, no clutch key, cannot drive a motorway). `founder-batch.md` §4 says it outright: "Those are engineering, not a ruling. They do not need you."\n- **`91_LAUNCH_BLOCKERS` §4.10–§4.16 ops items** — Postgres `max_connections`, applying the three migrations, making autodeploy follow `staging-green`, logrotate, giving yourself an admin session. Blocked on having the key, not on judgement. (§4.9 `payments:reconcile` becomes a real per-row grant-or-refund decision the day **after** live keys exist — zero live rows today.)\n- **M-22 security headers, M-23 the unauthenticated `/checkout/return`, M-24 consent versioning, audio/mute control.** Real defects; an engineer fixes them without asking you.\n- **The €125 Galaxy A16 device gate** — already retired on your instruction (doc 82 §2.4: "the founder is the test device"). Mentioned only so nobody re-raises it.\n- **`docs/development/CLAIMS.md` §"Needs a decision (founder)" and doc 64 §5.** Both dated 2026-07-20 and demonstrably stale — one row asks you to review "726 draft theory questions" and there are **0 drafts** today. Not a decision list; a housekeeping job. I will re-verify each row against the tree and re-file whatever survives, ~45 min, no input from you.\n\n# THREE TRAPS THAT WILL COST YOU TIME IF NOBODY TELLS YOU\n\n1. **`/review` 404s in production.** `platform/src/app/(dashboard)/review/page.tsx:37` — `if (process.env.NODE_ENV === "production") notFound();`. You can only sign on a dev machine: `cd platform && npm run dev`, then `/review` as the admin account. **Staging will not let you sign.**\n2. **There is no bulk-approve endpoint, on purpose** — it was deleted because "a signature minted that way says a human read thirty rows they never opened, which is the same lie as a generator writing `status: approved`". Budget ~2–3 min per row.\n3. **`content/review/FLAGGED-FOR-REVIEW.md` is badly stale** — written after "Вълна 2" on 2026-07-07, it lists 188 ids, misses 230 of today's 293 `needs-review` rows and names 125 that no longer need review. Do not use it as a worklist; use `/review`.\n\n# SEQUENCE THAT KEEPS THE DECISIONS HONEST\n\n- **4 before 3** — signing `q-ptp-022` *is* taking the recovery-position ruling, silently.\n- **30 before 3** — the БЧК letter has weeks of lead time and two of the 29 rows rest on it.\n- **3 before 28** — `freeze-lesson-carry.mjs --propose` only then prints which concepts became eligible.\n- **7 and 9 before 8, 10, 13** — the entity and the domain are the two roots; almost everything in the money chain hangs off one of them.\n- **11 before 13** — you cannot sign the right DPAs until you know where production runs.\n- **If you only do three things today:** rotate the SSH key (20 min), rotate `founder@knijka.ai` with the `sessionEpoch` bump (10 min, commands already written verbatim), and start the two slow external clocks — Stripe activation and the lawyer — because those are the only two where the waiting is someone else's."
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Sweep"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Synthesise"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "sweep:content-signoff",
      "phaseIndex": 1,
      "phaseTitle": "Sweep",
      "agentId": "a5170ba8aec43961f",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1789031569317,
      "queuedAt": 1789031569276,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SWEEP: the content sign-off surface. Everything below is me…",
      "promptPreview": "Work at E:/AI driver — a Bulgarian driving-theory + simulator product for 17-year-olds, built by a\nsolo founder with Claude. A long audit programme has been closing defects; 1,511 were filed and\nmost are retired. The founder has just asked, precisely:\n\n  "What questions stand in my way to decide what to do with them, how many are they, and what\n   exactly do they need my approval/decision for?"\n\nH…",
      "lastProgressAt": 1789032551338,
      "tokens": 191439,
      "toolCalls": 99,
      "durationMs": 982016,
      "resultPreview": "{"items":[{"title":"Sign the 29 first-aid questions — the clinical ruling","decision":"Approve, reject or rewrite the 29 medical rows in content/questions/ptp-i-parva-pomosht.json. Four concepts sit at ZERO approved (c-first-aid-priorities 0/9, c-cpr-basics 0/6, c-bleeding-control 0/7, c-victim-handling 0/7), so nothing partial helps. Second half of the decision: does a clinician read them before …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "sweep:audit-filings",
      "phaseIndex": 1,
      "phaseTitle": "Sweep",
      "agentId": "af0c3929609684739",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1789031569318,
      "queuedAt": 1789031569276,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SCOPE OF THIS SWEEP: rulings the audit programme filed agai…",
      "promptPreview": "Work at E:/AI driver — a Bulgarian driving-theory + simulator product for 17-year-olds, built by a\nsolo founder with Claude. A long audit programme has been closing defects; 1,511 were filed and\nmost are retired. The founder has just asked, precisely:\n\n  "What questions stand in my way to decide what to do with them, how many are they, and what\n   exactly do they need my approval/decision for?"\n\nH…",
      "lastProgressAt": 1789033065691,
      "tokens": 268056,
      "toolCalls": 117,
      "durationMs": 1495226,
      "resultPreview": "{"items":[{"title":"Sign the 33 questions sitting at needs-review in ПТП и първа помощ","decision":"Approve the 33 `needs-review` rows in the ПТП/first-aid bank (or name what else you want checked first). 29 of them are the first-aid block whose sources were already retrieved (ERC Guidelines 2021 BLS, Resuscitation 2021;161:98-114); they were `approved` and were deliberately moved back because eve…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "sweep:register-and-docs",
      "phaseIndex": 1,
      "phaseTitle": "Sweep",
      "agentId": "a445b80fa1cd7a7ba",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1789031569320,
      "queuedAt": 1789031569276,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SCOPE OF MY SWEEP: the founder item register (`docs/simulat…",
      "promptPreview": "Work at E:/AI driver — a Bulgarian driving-theory + simulator product for 17-year-olds, built by a\nsolo founder with Claude. A long audit programme has been closing defects; 1,511 were filed and\nmost are retired. The founder has just asked, precisely:\n\n  "What questions stand in my way to decide what to do with them, how many are they, and what\n   exactly do they need my approval/decision for?"\n\nH…",
      "lastProgressAt": 1789032805064,
      "tokens": 237317,
      "toolCalls": 99,
      "durationMs": 1234599,
      "resultPreview": "{"items":[{"title":"Sign the theory bank — 0 of 1,089 questions carry a human signature","decision":"Whether to start signing content at /review and in what order. No agent may write `approved`; the signature file is the only proof a person read a row.","options":["Sign in topic batches, cleanest-first (doc 65 §4 orders them)","Delegate to a named reviewer — their name goes in the `by` field, whic…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "sweep:security-and-authority",
      "phaseIndex": 1,
      "phaseTitle": "Sweep",
      "agentId": "ab24fd0cffdd93792",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1789031569322,
      "queuedAt": 1789031569277,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SWEEP: security, accounts, and real-world authority. 20 ite…",
      "promptPreview": "Work at E:/AI driver — a Bulgarian driving-theory + simulator product for 17-year-olds, built by a\nsolo founder with Claude. A long audit programme has been closing defects; 1,511 were filed and\nmost are retired. The founder has just asked, precisely:\n\n  "What questions stand in my way to decide what to do with them, how many are they, and what\n   exactly do they need my approval/decision for?"\n\nH…",
      "lastProgressAt": 1789032515726,
      "tokens": 135353,
      "toolCalls": 45,
      "durationMs": 945260,
      "resultPreview": "{"items":[{"title":"Rotate the FlokiNET VPS SSH key — its private half was surfaced in a chat session and it still works","decision":"Generate a new ed25519 keypair on the dev box, install the new public key in the VPS root authorized_keys, remove the old public key, then delete the old private key file. Also decide whether the exposure warrants treating the box as compromised (log review / rebuil…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "synthesise",
      "phaseIndex": 2,
      "phaseTitle": "Synthesise",
      "agentId": "a297933adc9a5eece",
      "model": "claude-opus-5",
      "state": "done",
      "startedAt": 1789033065711,
      "queuedAt": 1789033065706,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd "/e/AI driver" && echo "--- claim-ledger tracked? ---"; …",
      "promptPreview": "Work at E:/AI driver — a Bulgarian driving-theory + simulator product for 17-year-olds, built by a\nsolo founder with Claude. A long audit programme has been closing defects; 1,511 were filed and\nmost are retired. The founder has just asked, precisely:\n\n  "What questions stand in my way to decide what to do with them, how many are they, and what\n   exactly do they need my approval/decision for?"\n\nH…",
      "lastProgressAt": 1789033701355,
      "tokens": 149343,
      "toolCalls": 19,
      "durationMs": 635644,
      "resultPreview": "# THE FOUNDER DECISION LIST — 2026-09-10\n\n**38 decisions are actually waiting on you.**\n\n- **15 block launch** (nothing sells, or nothing ships honestly, until you answer)\n- **12 block closing audit rows** — 34 of the 116 open findings, verified open today, cannot close in either direction without you\n- **11 are neither, but are cheap and get more expensive with time**\n\n**How I verified:** every i…"
    }
  ],
  "totalTokens": 981508,
  "totalToolCalls": 379
}